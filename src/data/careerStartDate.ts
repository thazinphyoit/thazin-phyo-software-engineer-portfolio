/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

// Kept in its own module (no other imports) so vite.config.ts can depend on it
// without pulling in asset imports (e.g. the profile picture in portfolioData.ts).
export const CAREER_START_DATE = "2019-05-02";
